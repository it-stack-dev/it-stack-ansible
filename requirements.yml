---
# Ansible Galaxy roles and collections required by it-stack-ansible.
# Install with: ansible-galaxy install -r requirements.yml
#           or: make deps

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: community.postgresql
    version: ">=3.0.0"
  - name: community.crypto
    version: ">=2.0.0"
  - name: ansible.posix
    version: ">=1.5.0"

roles:
  - name: geerlingguy.java
    version: "2.1.0"
