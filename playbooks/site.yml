---
# playbooks/site.yml
# Master playbook for the complete IT-Stack Phase 1 deployment.
# Run with: make site
# Or:       ansible-playbook -i inventory/hosts.ini --vault-password-file .vault_pass playbooks/site.yml
#
# Dependency order — do not reorder:
#   1. common      (all servers — base OS hardening)
#   2. freeipa     (lab-id1 — DNS must be up before other servers can resolve)
#   3. postgresql  (lab-db1 — databases must exist before app services start)
#   4. redis       (lab-db1 — cache layer alongside PostgreSQL)
#   5. keycloak    (lab-id1 — SSO layer needs IPA + PG)
#   6. traefik     (lab-proxy1 — routing layer last)

- name: "Phase 1.0 — Common base setup (all servers)"
  hosts: phase1
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: common
      tags: [always, common]

- name: "Phase 1.1 — FreeIPA identity server"
  hosts: identity
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: freeipa
      tags: [identity, freeipa]

- name: "Phase 1.2 — PostgreSQL database server"
  hosts: database
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: postgresql
      tags: [database, postgresql]

- name: "Phase 1.3 — Redis cache server"
  hosts: database
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: redis
      tags: [database, redis]

- name: "Phase 1.4 — Keycloak SSO broker"
  hosts: identity
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: keycloak
      tags: [identity, keycloak]

- name: "Phase 1.5 — Traefik reverse proxy"
  hosts: infrastructure
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: traefik
      tags: [infrastructure, traefik]

# ============================================================
# Phase 2: Collaboration
# Dependency order: nextcloud → mattermost → jitsi
#   (Elasticsearch must be up before Zammad in Phase 3)
# ============================================================

- name: "Phase 2.0 — Elasticsearch search & analytics engine"
  hosts: database
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: elasticsearch
      tags: [database, elasticsearch]

- name: "Phase 2.1 — Nextcloud file sharing & collaboration"
  hosts: collaboration
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: nextcloud
      tags: [collaboration, nextcloud]

- name: "Phase 2.2 — Mattermost team messaging"
  hosts: collaboration
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: mattermost
      tags: [collaboration, mattermost]

- name: "Phase 2.3 — Jitsi Meet video conferencing"
  hosts: collaboration
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: jitsi
      tags: [collaboration, jitsi]

- name: "Phase 2.4 — iRedMail email server"
  hosts: communications
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: iredmail
      tags: [communications, iredmail, email]

- name: "Phase 2.5 — Zammad help desk"
  hosts: communications
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: zammad
      tags: [communications, zammad, helpdesk]

# ============================================================
# Phase 3: Back Office
# ============================================================

- name: "Phase 3.0 — FreePBX VoIP PBX"
  hosts: communications
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: freepbx
      tags: [voip, freepbx, communications]

- name: "Phase 3.1 — SuiteCRM customer relationship management"
  hosts: business
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: suitecrm
      tags: [business, suitecrm, crm]

- name: "Phase 3.2 — Odoo ERP platform"
  hosts: business
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: odoo
      tags: [business, odoo, erp]

- name: "Phase 3.3 — OpenKM document management system"
  hosts: business
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: openkm
      tags: [business, openkm, dms]

# ============================================================
# Phase 4: IT Management & Observability
# ============================================================

- name: "Phase 4.0 — Taiga project management"
  hosts: it_management
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: taiga
      tags: [it-management, taiga, projects]

- name: "Phase 4.1 — Snipe-IT asset management"
  hosts: it_management
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: snipeit
      tags: [it-management, snipeit, assets]

- name: "Phase 4.2 — GLPI IT service management"
  hosts: it_management
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: glpi
      tags: [it-management, glpi, itsm]

- name: "Phase 4.3 — Zabbix infrastructure monitoring"
  hosts: communications
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: zabbix
      tags: [monitoring, zabbix, infrastructure]

- name: "Phase 4.4 — Graylog centralized log management"
  hosts: infrastructure
  gather_facts: true
  vars_files:
    - ../vault/secrets.yml
  roles:
    - role: graylog
      tags: [logging, graylog, infrastructure]
