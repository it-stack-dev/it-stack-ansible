---
# roles/nextcloud/meta/main.yml

galaxy_info:
  role_name: nextcloud
  author: it-stack-dev
  description: Deploy Nextcloud file sync and collaboration platform
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - "24.04"
  galaxy_tags:
    - nextcloud
    - files
    - collaboration
    - it-stack

dependencies:
  - role: common