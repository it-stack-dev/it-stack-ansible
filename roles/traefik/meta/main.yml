---
# roles/traefik/meta/main.yml

galaxy_info:
  role_name: traefik
  author: it-stack-dev
  description: Deploy and configure Traefik reverse proxy for IT-Stack services
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - "24.04"
  galaxy_tags:
    - traefik
    - proxy
    - tls
    - infrastructure
    - it-stack

dependencies:
  - role: common
