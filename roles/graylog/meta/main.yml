---
# roles/graylog/meta/main.yml

galaxy_info:
  role_name: graylog
  author: it-stack-dev
  description: Deploy Graylog centralized log management
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - "24.04"
  galaxy_tags:
    - graylog
    - logging
    - siem
    - it-stack

dependencies:
  - role: common